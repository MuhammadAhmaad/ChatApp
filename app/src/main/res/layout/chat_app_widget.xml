<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimaryDark"
    android:padding="@dimen/widget_margin">

    <TextView
        android:id="@+id/appwidget_user_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/appwidget_text"
        android:textColor="#ffffff"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_centerInParent="true"
        android:layout_below="@id/appWidget_profile_picture"
        android:layout_marginStart="20dp"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/appwidget_user_status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#ffffff"
        android:textSize="18sp"
        android:textStyle="bold|italic"
        android:layout_marginStart="20dp"
        android:layout_centerInParent="true"
        android:layout_below="@id/appwidget_user_name"
        app:layout_constraintTop_toBottomOf="@id/appwidget_user_name" />

    <ImageView
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/appWidget_profile_picture"
        android:layout_width="180dp"
        android:layout_height="180dp"
        android:contentDescription="@string/widget_profile_image"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"/>


</RelativeLayout>